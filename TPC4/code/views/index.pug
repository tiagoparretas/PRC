extends layout

block content
  .w3-content
    header.w3-container.w3-center.w3-padding-48.w3-black
      h1.w3-xxxlarge Obras Musicais
  .w3-container
    table.w3-table-all.w3-card-4
      tr
        th ID
        th Título
        th Partituras
        th Detalhe
      each obra in obras
        tr
          td= obra.id
          td= obra.tit
          td= obra.count
          td 
            a(href="/" + obra.id) 
              button Ver
